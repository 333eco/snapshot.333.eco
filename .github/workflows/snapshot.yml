name: Daily full snapshot

# The daily full refresh of the estate's Internet Archive captures — the half of
# the prior-art snapshot that used to run in every site repository, once a week.
#
# CADENCE: daily at 06:00 UTC, raised from weekly on 2026-09-05 because the run
# is free here. What the cadence buys is the corpus: a paper revised in the
# publications repositories is captured by no push job (those repositories have
# none), so its Wayback lag was up to seven days and is now one. Not every other
# day, because "*/2" in cron is day-of-month arithmetic and skips a beat at every
# 31-day month; daily is a property, every-other-day is a rule with an exception.
# The Archive stores an unchanged page as a revisit record, so the cost to it of a
# daily request for an unchanged URL is the request.
#
# WHY ONE PUBLIC REPOSITORY. Actions minutes bill the OWNER of a private
# repository, and the same Sunday job in 22 private repositories cost about 580
# minutes a month, most of it a runner sleeping between rate-limited requests.
# In a public repository the identical job costs nothing. Nothing here is
# secret: every URL is public by definition (it is being sent to a public
# archive), each site's sitemap.xml is public, the corpora are public, and Save
# Page Now takes no credential. So the saving is a property of where the job
# runs, not a budget somebody has to keep watching.
#
# WHAT IT READS, and why nothing is hand-listed that can be derived:
#   hosts.txt       -> https://<host>/sitemap.xml, LIVE — the deployed sitemap is
#                      the set of public routes, and it can be ahead of any
#                      checkout (thonly.org's live one listed 180 routes to the
#                      working tree's 134 on the day this was written).
#   corpora.txt     -> every tracked *.md in each public corpus repository, minus
#                      README/TIMESTAMPS/ZENODO. The hand-kept manifests were 12
#                      papers behind the tree when this replaced them.
#   extra-urls.txt  -> what cannot be derived: pages kept out of a sitemap. Not
#                      redirects — Save Page Now records a redirect's target, not
#                      the 301, so a redirect is captured once, when it ships.
# Each site repository's own snapshot.yml checks on every push that its host is
# listed here and that every URL in its manifest is reachable by this run — so
# drift shows up red in the repository that drifted.
#
# THE PUSH HALF STAYS WHERE IT WAS. A site repository's snapshot.yml still
# submits, on push, the pages that push changed — it needs the source tree to
# know which, and after the 2026-09-05 fix that is about a minute.
#
# THE KEEP-ALIVE. GitHub pauses a scheduled workflow after 60 days without
# repository activity. This repository is rarely touched by hand, so the last
# job rewrites LAST-RUN.md and commits it: the run itself is the activity that
# keeps the run scheduled. Its content is the record of what happened.
#
# THE ARCHIVE.ORG KEY (optional). With the repository secrets IA_S3_ACCESS_KEY and
# IA_S3_SECRET_KEY set — an archive.org account's S3-style pair, from
# https://archive.org/account/s3.php — submit.sh uses the Save Page Now 2 API:
# authenticated, asynchronous (a job id back in a second instead of a held
# connection), with if_not_archived_within so a page a push captured earlier the
# same day is not captured twice. Without them it uses anonymous Save Page Now,
# exactly as before. A key that is set but rejected degrades to anonymous with a
# warning, never to 400 errors; the summary says which path ran. The key lives
# ONLY here: the site repositories' push jobs stay secret-free by design, and
# this repository has no pull_request trigger, so a fork cannot reach it.
#
# RATE LIMITS. Each host is its own job. Anonymously that meant one IP per job,
# which is how the 22 per-repo jobs already behaved. Authenticated, every job
# shares ONE account's few concurrent capture slots (3 on the first run), so
# submit.sh waits for a free slot before each request and retries on the
# session-limit error; max-parallel is kept at the slot count so the jobs mostly
# take turns instead of colliding. thonly.org alone is ~180 routes at ~12 s
# each — about 40 minutes — which is why the per-job timeout is generous.

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
    inputs:
      host:
        description: "Run only this host (blank = every host in hosts.txt)"
        required: false
        default: ""
      corpora:
        description: "Also submit the corpus sources"
        type: boolean
        default: true

permissions:
  contents: write # the record job commits LAST-RUN.md

concurrency:
  group: full-snapshot
  cancel-in-progress: false

jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      hosts: ${{ steps.plan.outputs.hosts }}
    steps:
      - uses: actions/checkout@v4
      - id: plan
        env:
          ONLY: ${{ github.event.inputs.host }}
        run: |
          set -uo pipefail
          if [ -n "$ONLY" ]; then
            HOSTS="$(printf '%s' "$ONLY" | tr -d ' ')"
          else
            HOSTS="$(sed 's/#.*//' hosts.txt | tr -d ' \t' | grep -v '^$')"
          fi
          JSON="$(printf '%s\n' $HOSTS | jq -R . | jq -sc .)"
          echo "hosts=$JSON" >> "$GITHUB_OUTPUT"
          echo "Hosts: $JSON"

  hosts:
    needs: plan
    runs-on: ubuntu-latest
    timeout-minutes: 240
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        host: ${{ fromJSON(needs.plan.outputs.hosts) }}
    steps:
      - uses: actions/checkout@v4
      - name: Submit every public route of ${{ matrix.host }}
        env:
          HOST: ${{ matrix.host }}
          IA_S3_ACCESS_KEY: ${{ secrets.IA_S3_ACCESS_KEY }}
          IA_S3_SECRET_KEY: ${{ secrets.IA_S3_SECRET_KEY }}
        run: ./submit.sh host "$HOST"

  corpora:
    needs: plan
    if: github.event.inputs.corpora != 'false'
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4
      - name: Submit every tracked paper in the public corpora
        env:
          IA_S3_ACCESS_KEY: ${{ secrets.IA_S3_ACCESS_KEY }}
          IA_S3_SECRET_KEY: ${{ secrets.IA_S3_SECRET_KEY }}
        run: ./submit.sh corpora

  record:
    needs: [hosts, corpora]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Write LAST-RUN.md and commit it
        env:
          HOSTS_RESULT: ${{ needs.hosts.result }}
          CORPORA_RESULT: ${{ needs.corpora.result }}
        run: |
          set -uo pipefail
          {
            echo "# Last daily full snapshot"
            echo ""
            echo "- **Ran:** $(date -u '+%Y-%m-%d %H:%M UTC') (\`${GITHUB_EVENT_NAME}\`)"
            echo "- **Hosts job:** ${HOSTS_RESULT}"
            echo "- **Corpora job:** ${CORPORA_RESULT}"
            echo "- **Run:** ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
            echo ""
            echo "_Rewritten by every run. Its purpose is the commit: GitHub pauses a scheduled workflow after 60 days without repository activity, and this file is the activity. Per-URL results are in the run's job summaries._"
          } > LAST-RUN.md
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add LAST-RUN.md
          git commit -m "daily snapshot $(date -u +%Y-%m-%d): hosts ${HOSTS_RESULT}, corpora ${CORPORA_RESULT}" || { echo "Nothing to commit."; exit 0; }
          git pull --rebase --autostash
          git push
