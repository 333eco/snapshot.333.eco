# URLs the daily run cannot derive: not in any live sitemap and not a corpus
# source. One per line.
#
# ⛔ NOT redirects. Save Page Now follows a redirect and records the TARGET, not
# the 301: the twelve heartbank.net/research/* redirects listed here until
# 2026-09-05 have 301 rows in the CDX from June only, and every later submission
# captured thonly.org's page instead. A redirect is captured ONCE, the day it
# ships — submit it then and confirm a 301 row with
#   https://web.archive.org/cdx/search/cdx?url=<old-url>&fl=timestamp,statuscode
# Listing it here afterwards adds nothing.

# thonly.org — listed in its manifest, deliberately outside its sitemap
https://thonly.org/research/template
